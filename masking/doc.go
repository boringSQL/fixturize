// Package masking holds the data-masking-policy.yml schema and the
// loader/discovery used by fixturize. Stdlib + yaml only, so other
// tools can read the same file without pulling the rest of fixturize.
package masking
